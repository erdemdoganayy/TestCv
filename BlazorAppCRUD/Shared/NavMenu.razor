<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <img width=50 height=30 src="https://www.hepapi.com/assets/img/hepapilogo.png"/><a class="navbar-brand" href="">  CV MAKER</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

@inject Blazored.LocalStorage.ILocalStorageService oLocalStore
@inject NavigationManager NavigationManager
@inject IJSRuntime jsRuntime

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
       @* asda <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="counter">
                <span class="oi oi-plus" aria-hidden="true"></span> Counter
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="fetchdata">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Fetch data
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="home">
                <span class="oi oi-flag" aria-hidden="true"></span> CV
            </NavLink>
        </div>*@
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="EmployeeAdd">
                <span class="oi oi-plus" aria-hidden="true"></span> Add Employee
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="Login">
                <span class="oi oi-account-logout" @onclick="@Logout" aria-hidden="true"></span> Logout
            </NavLink>
 
        </div>
    </nav>
</div>


@code {
    private bool collapseNavMenu = true;
    string SessionValue { get; set; }

    public async void Logout()
    {
        SessionValue = await oLocalStore.GetItemAsync<string>("MySessionValue");
        await oLocalStore.ClearAsync();
        SessionValue = "";
    }
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
